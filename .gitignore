# Knowledge base / personal files (contains PII, API keys, conversation transcripts)
kb/

# Python build artifacts
build/
dist/
*.egg-info/
__pycache__/
*.pyc
*.pyo

# Virtual environments
.venv/
venv/

# IDE / editor
.vscode/
.idea/
*.swp
*.swo
*~

# OS files
Thumbs.db
Desktop.ini
.DS_Store

# Environment / secrets
.env
*.key
*.pem

# Phase-2 exit-gate run artifacts (audit reports + isolated PROV stores)
exit-gate-runs/

# Phase-3 validation source-client cache (content-addressed, machine-local runtime state)
runtime/validation-cache/

# Phase-3 machine-local calibration override (e.g. an in-progress S3-cal fit not yet reviewed for
# commit). The pinned runtime/validation-calibration.json IS committed -- this is only for a
# not-yet-official local variant a session might write while iterating on a Platt refit.
runtime/validation-calibration.local.json
